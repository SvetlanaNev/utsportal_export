import { LoginForm } from "@/components/login-form";

export default function Home() {
  return (
    <main className="flex h-screen w-screen items-center justify-center bg-red-500">
      <h1 className="text-4xl font-bold text-white">
        If you see this red screen, the build process is working.
      </h1>
    </main>
  );
}
